name: Deploy Next.js to Azure Web App

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Setup Node.js (stable version)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Install dependencies
      - name: Install dependencies
        run: npm ci

      # 4. Build Next.js app (optional but useful to fail fast)
      - name: Build Next.js app
        run: npm run build
        env:
          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}

      # 5. Create deployment package (deploy source; let Oryx build on App Service)
      - name: Create deployment package
        run: |
          zip -r app.zip . \
            -x "node_modules/**" \
            -x ".git/**" \
            -x ".next/**" \
            -x ".github/**" \
            -x ".vscode/**" \
            -x ".env*" \
            -x "app.zip"

      # 6. Login to Azure
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # 7. Deploy to Azure Web App
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: medicology
          package: app.zip
