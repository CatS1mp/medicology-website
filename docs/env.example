# Next.js Website environment example (place in project root as .env.local or .env)
NODE_ENV=development
PORT=3000
NEXT_TELEMETRY_DISABLED=1

# API base URL (Gateway)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Optional auth / cookie names (tuỳ triển khai)
# AUTH_COOKIE_NAME=medicology_auth
